{
  "name": "logify-docs",
  "version": "0.1.0",
  "private": true,
  "description": "VitePress documentation site for Logify / logkit (deployed via GitHub Pages).",
  "type": "module",
  "scripts": {
    "docs:dev": "vitepress dev",
    "docs:build": "vitepress build",
    "docs:preview": "vitepress preview",
    "lint": "echo \"No JS/TS source to lint in this package (Markdown-only docs site).\"",
    "typecheck": "echo \"No JS/TS source to type-check in this package (Markdown-only docs site).\"",
    "test": "echo \"No tests defined for this package (Markdown-only docs site).\"",
    "build": "npm run docs:build"
  },
  "devDependencies": {
    "vitepress": "1.6.4"
  },
  "overrides": {
    "vite": "^6.4.3",
    "esbuild": "^0.25.0"
  }
}
