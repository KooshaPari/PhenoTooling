# Status: Active (maintenance mode)

> **This file previously stated the package was migrated to `libs/config-ts/`.
> That migration did not complete. This package (`@phenotype/config-ts`) is the
> canonical implementation and is actively maintained here.**

## Current state

- Package: `@phenotype/config-ts` v0.1.0
- Location: `typescript/packages/conft/` (this directory)
- Status: active, receiving bug fixes and feature work
- npm publish: not yet automated (planned — see `PLAN.md`)

## Planned future work

- Automated npm publish via `.github/workflows/npm-publish.yml`
- Coverage ratchet enforcement in CI
- Vitest v1 → v2 upgrade
- Hot-reload adapter (file-watcher integration)
- Rust crate (`configkit`) — not yet started
