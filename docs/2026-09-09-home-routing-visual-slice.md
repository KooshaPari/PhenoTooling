# Home, routing and NetWeave visual slice

2026-09-09 Pacific. Bounded design/implementation plan for the user-approved visible slice. This extends the existing Technical Atelier; it does not certify the full redesign or authorize deployment.

## Confirmed design context

Direct user source29346 requests a technically impressive, dynamic, interactive portfolio with 3D keyboards, animated terminals/type, dimensional assets and rich UI while retaining the flat Linux/typewriter quality. Direct user source25340 distinguishes personal, Phenotype and project brands across recruiting, engineering/product, B2B, B2C and reputation, including blind and informed audiences. These are recovered user statements, not inferred from code.

Parent `port-pmp.md` V3.1 preserves paper/ink/grid/mono, shared records, case-study copy and factual boundaries. V3.2 requires a coherent inspectable studio artifact and complete Reader path. V3.3 requires project-specific proofs: OmniRoute is compact attributed upstream evidence, NetWeave is a bounded local-rule study with future rerouting explicitly excluded. V3.4 requires original/rights-aware assets and reproducible derivatives; V3.5 requires mobile and reduced-motion alternatives.

Existing approval covers the visible Home/OmniRoute/NetWeave slice. No new brand questionnaire is needed. This plan settles routine composition details within that approval; implementation starts after coordinator ownership agreement.

## Layout choice

Recommended: an editorial opening with a larger proof area and a tightly paired, varied study grid below. Keep the WITF original photo as the material anchor. Remove empty grid-row staging so systems and research studies sit alongside each other at desktop widths. Retain distinct artifact treatments; avoid turning everything into identical cards.

Alternatives considered: a uniform equal-card grid would reduce gaps but flatten the requested artifact distinctions; a new fullscreen immersive stage would require substantially broader interaction/asset work. The recommended composition improves this existing slice while preserving room for the larger rich-artifact program.

## Concrete Home changes

1. Keep existing identity wording and all project records. Balance desktop opening columns so the headline has sufficient width and the WITF proof remains prominent. Reduce excessive opening/section spacing using existing spacing tokens or bounded fluid values.
2. At mobile, reduce the oversized headline/intro stack and vertical gaps, bringing the first authentic artifact earlier without hiding audience choices, evidence or copy. Preserve normal document reading order.
3. Replace the selected-study grid's explicit stagger/row placement with compact natural-flow paired columns, using a wider systems column and narrower experiment column where space permits. Retain differentiated physical specimen widths where appropriate; no empty row should exist only for decorative placement.
4. Use Home-scoped styles for consistent figure/caption spacing and tighter related annotations. Preserve accessible focus, 44px interactive targets and existing Reader behavior. No ambient animation is added merely to disguise layout problems.
5. Leave room for project-owned rich artifacts: this layout pass does not claim a still photo is an inspectable 3D object, or an illustration is recorded project output.

## Cross-worker artifact contracts

| Surface | Required result | Ownership boundary |
| --- | --- | --- |
| OmniRoute Home artifact | Routing-specific, compact upstream-contribution proof; no reused ShareCLI agent/queue/host diagram | Separate artifact worker owns `scripts/components/artifact.js` and its regression |
| NetWeave | Legible directed-graph/local-rule topology; one coherent traffic inspection state/control story; honest illustrative labels and still fallback | Separate NetWeave worker owns diagram/media/control implementation |
| Home | Earlier proof, compact intentional study rhythm, preserved lens ordering and readable mobile flow | This worker owns `scripts/views/home.js`; only `.home-*`-scoped rules in `styles/artifacts.css` and `styles/responsive.css`, subject to coordinator confirmation |

No shared global typography, shell navigation, project data, other media modules, resume, Git or deployment changes belong to this lane. Other workers are active; their edits must be preserved.

## Acceptance

Inspect fresh desktop1440x1000 and mobile390x844 Home captures in both lenses, plus Reader mode. Verify all existing project links and content remain present, no horizontal overflow, logical DOM/tab order, and materially reduced empty desktop rows. Confirm first authentic proof appears earlier on mobile than the baseline without clipping or hiding copy. Review screenshot differences physically.

For the combined slice, inspect OmniRoute's actual topology labels and NetWeave's diagram/control states; retain its no-rerouting boundary. Separate workers provide their focused regression evidence. Coordinator runs shared build/browser gates after changes are integrated, avoiding competing writes to staged output.

Baseline screenshots already inspected: `output/playwright/2026-09-09-home-desktop.png`, `2026-09-09-home-mobile.png`, `2026-09-09-netweave-desktop.png`, `2026-09-09-netweave-mobile.png`, `2026-09-09-netweave-state2.png`, `2026-09-09-netweave-state3.png`. They document the pre-slice presentation, not current production.

## Status

PLAN READY FOR COORDINATION. No Home source changes made by this plan. Full flagship asset production, broader Workbench, performance/accessibility matrix, canonical custody and hosted release remain separate open gates.
